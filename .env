# Django settings
DEBUG=True
SECRET_KEY=your-super-secret-django-key
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL settings (used by Django to connect)
DB_NAME=library_db
DB_USER=root
DB_PASSWORD=root@123
DB_HOST=db
DB_PORT=3306

# Optional: MySQL container root password (used inside docker-compose)
DB_ROOT_PASSWORD=root@123
